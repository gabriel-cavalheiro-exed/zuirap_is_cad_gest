# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: br.com.gamma.zuirapiscadgestao
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://my430374.s4hana.cloud.sap/
          authenticationType: reentranceTicket # SAML support for vscode
        app:
          name: ZUIRAPISCADGEST
          description: Inside Sales - App RAP - Cadastro Gestão
          package: ZRAP_CADASTRO_GESTAO
          transport: BWPK900098
        exclude:
          - /test/
